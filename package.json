{
  "private": true,
  "workspaces": [
    "core",
    "example-app"
  ],
  "scripts": {
    "start": "concurrently --kill-others 'cd ./core && yarn watch' 'cd example-app && yarn start'"
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}